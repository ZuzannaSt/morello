{% extends '::base.html.twig' %}

{% block body %}
    {% for flashMessage in app.session.flashbag.get('success') %}
        <div class="flash-success">
            {{ flashMessage }}
        </div>
    {% endfor %}

    <h1>{{ 'Add Task'|trans }}</h1>

    <div>{{ form_errors(form) }}</div>
    {{ form(form, {'method': 'POST'}) }}

{% endblock %}
