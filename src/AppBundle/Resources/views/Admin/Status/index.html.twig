{% extends 'AppBundle:Admin:navbar.html.twig' %}

{% block title %}{{ 'admin.status.index.title' | trans({}, 'admin') }}{% endblock %}

{% block content -%}
    <div class="container">
        <h1>{{ 'Status list'|trans }}</h1>
        {% if status|length > 0 %}
            <table class="records_list">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Name</th>
                    </tr>
                </thead>
                <tbody>
                {% for status in status %}
                    <tr>
                        <td><a href="{{ path('admin_status_view', { 'id': role.id }) }}">{{ role.id }}</a></td>
                        <td>{{ role.name }}</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        {% endif %}

            <ul>
            <li>
                <a href="{{ path('admin_status_add') }}">
                    Create a new entry
                </a>
            </li>
        </ul>
    </div>
{% endblock %}
