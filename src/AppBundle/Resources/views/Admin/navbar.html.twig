{% extends 'base.html.twig' %}

{% block body %}
    <div class="navbar">
        <div class="container">
            <div class="navbar-header">
                <a href="{{ path('admin_dashboard') }}" class="navbar-brand">{{ 'admin.menu.brand' | trans({}, 'admin') }}</a>
            </div>
            <div class="collapse navbar-collapse">
                {% block menu %}
                    <ul class="nav navbar-nav">
                        <li><a href="{{ path('admin_dashboard') }}">{{ 'admin.menu.dashboard' | trans({}, 'admin') }}</a></li>
                        <li><a href="{{ path('admin_users_index') }}">{{ 'admin.menu.users' | trans({}, 'admin') }}</a></li>
                        <li><a href="{{ path('roles') }}">{{ 'admin.menu.roles' | trans({}, 'admin') }}</a></li>
                        <li><a href="{{ path('projects') }}">{{ 'admin.menu.projects' | trans({}, 'admin') }}</a></li>
                    </ul>
                {% endblock menu %}
                <ul class="nav navbar-nav navbar-right">
                    <li><a href="#">{{ 'app.menu.logged_as' | trans({'%email%':app.user.email}, 'application') }}</a></li>
                    <li><a href="{{ path('user_dashboard') }}">{{ 'admin.menu.user' | trans({}, 'admin')}}</a></li>
                    <li><a href="{{ path('logout') }}">{{ 'logout' | trans({}, 'admin') }} <i class="glyphicon glyphicon-off"></i></a></li>
                </ul>
            </div>
        </div>
    </div>
    <div class="container">
        <div class="row">
            {% for flashMessage in app.session.flashbag.get('success') %}
                {{ include('AppBundle:Flash:flash_success.html.twig', { 'message': flashMessage }) }}
            {% endfor %}
            {% for flashMessage in app.session.flashbag.get('info') %}
                {{ include('AppBundle:Flash:flash_info.html.twig', { 'message': flashMessage }) }}
            {% endfor %}
            {% for flashMessage in app.session.flashbag.get('warning') %}
                {{ include('AppBundle:Flash:flash_warning.html.twig', { 'message': flashMessage }) }}
            {% endfor %}
            {% for flashMessage in app.session.flashbag.get('error') %}
                {{ include('AppBundle:Flash:flash_error.html.twig', { 'message': flashMessage }) }}
            {% endfor %}
        </div>
        {% block content %}
        {% endblock content %}
    </div>
{% endblock body %}
